{
  "name": "set_step_running",
  "description": "Mark a persisted case step row as RUNNING.",
  "version": "0.1.0",
  "input_schema": {
    "type": "object",
    "properties": {
      "case_id": { "type": "string" },
      "step_db_row_id": { "type": "string" }
    },
    "required": ["case_id", "step_db_row_id"],
    "additionalProperties": false
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "request_id": { "type": "string" },
      "ok": { "type": "boolean" },
      "step_db_row_id": { "type": "string" }
    },
    "required": ["request_id", "ok", "step_db_row_id"],
    "additionalProperties": false
  },
  "entrypoint": "libs.tools.cases.tool:set_step_running",
  "env_vars": ["CASES_HTTP_URL"],
  "network_access": true,
  "timeout_ms": 10000,
  "max_memory_mb": 128
}
