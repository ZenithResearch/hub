Cases service API discovery: http://cases:8083 only exposes POST /cases (create case) via OpenAPI. No public dispatch endpoint found. Dispatch requires SSH to worker host or alternative gateway route. Always check /cases/openapi.json for available endpoints. Worker invocation uses: ssh <host> "HERMES_HOME=~/repos/hub/.hermes/workers/<worker> CASES_HTTP_URL=http://localhost:8083 hermes chat -q 'case_id=<id>'"
