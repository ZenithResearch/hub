diff --git a/agent/auxiliary_client.py b/agent/auxiliary_client.py
index da49a6951..0432ed543 100644
--- a/agent/auxiliary_client.py
+++ b/agent/auxiliary_client.py
@@ -4663,6 +4663,27 @@ def _normalize_resolved_model(model_name: Optional[str], provider: str) -> Optio
         return model_name
 
 
+def _enforce_strict_auxiliary_route(
+    provider: str,
+    model: str,
+    base_url: str,
+    api_key: str,
+) -> None:
+    if os.environ.get("HERMES_STRICT_LOCAL_MODEL_ROUTING") != "1":
+        return
+    expected_model = os.environ.get("HERMES_PINNED_MODEL", "").strip()
+    expected_base_url = os.environ.get("HERMES_PINNED_BASE_URL", "").strip().rstrip("/")
+    if (
+        provider != "custom"
+        or not expected_model
+        or model != expected_model
+        or not expected_base_url
+        or base_url != expected_base_url
+        or api_key not in {"", "no-key-required"}
+    ):
+        raise RuntimeError("local inference route mismatch")
+
+
 def resolve_provider_client(
     provider: str,
     model: str = None,
@@ -4715,6 +4736,29 @@ def resolve_provider_client(
     # Normalise aliases
     provider = _normalize_aux_provider(provider)
 
+    if os.environ.get("HERMES_STRICT_LOCAL_MODEL_ROUTING") == "1":
+        candidate_model = str(
+            model
+            or (main_runtime.get("model") if main_runtime else "")
+            or _read_main_model()
+        ).strip()
+        candidate_base_url = str(
+            explicit_base_url
+            or (main_runtime.get("base_url") if main_runtime else "")
+            or _read_main_base_url()
+        ).strip().rstrip("/")
+        candidate_api_key = str(
+            explicit_api_key
+            or (main_runtime.get("api_key") if main_runtime else "")
+            or ""
+        ).strip()
+        _enforce_strict_auxiliary_route(
+            provider,
+            candidate_model,
+            candidate_base_url,
+            candidate_api_key,
+        )
+
     # Universal model-resolution fallback for concrete providers. ``auto`` is
     # intentionally excluded: `_resolve_auto(main_runtime=...)` returns the
     # model paired with the provider it actually selected. Pre-filling an auto
diff --git a/gateway/run.py b/gateway/run.py
index f59e006f6..aa25f6275 100644
--- a/gateway/run.py
+++ b/gateway/run.py
@@ -2115,6 +2115,25 @@ def _resolve_runtime_agent_kwargs() -> dict:
     }
 
 
+def _enforce_strict_local_model_route(model: str, runtime: dict) -> tuple[str, dict]:
+    if os.environ.get("HERMES_STRICT_LOCAL_MODEL_ROUTING") != "1":
+        return model, runtime
+    expected_model = os.environ.get("HERMES_PINNED_MODEL", "").strip()
+    expected_base_url = os.environ.get("HERMES_PINNED_BASE_URL", "").strip().rstrip("/")
+    if (
+        not expected_model
+        or model != expected_model
+        or runtime.get("provider") != "custom"
+        or str(runtime.get("base_url") or "").strip().rstrip("/") != expected_base_url
+        or runtime.get("api_mode") not in {None, "chat_completions"}
+        or runtime.get("api_key") not in {None, "", "no-key-required"}
+        or runtime.get("command")
+        or runtime.get("args")
+    ):
+        raise RuntimeError("local inference route mismatch")
+    return model, runtime
+
+
 def _resolve_runtime_agent_kwargs_for_provider(provider: str) -> dict:
     """Resolve runtime credentials for a specific provider (e.g. from channel override)."""
     from hermes_cli.runtime_provider import (
@@ -4116,6 +4135,8 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
                 "credential_pool": override.get("credential_pool"),
             }
             if override_runtime.get("api_key"):
+                if os.environ.get("HERMES_STRICT_LOCAL_MODEL_ROUTING") == "1":
+                    return _enforce_strict_local_model_route(override_model, override_runtime)
                 if override_runtime.get("credential_pool") is None:
                     override_runtime["credential_pool"] = _credential_pool_for_provider(
                         override.get("provider")
@@ -4125,7 +4146,7 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
                     resolved_session_key or "", model, override_model,
                     override_runtime.get("provider"),
                 )
-                return override_model, override_runtime
+                return _enforce_strict_local_model_route(override_model, override_runtime)
             # Override exists but has no api_key — fall through to env-based
             # resolution and apply model/provider from the override on top.
             logger.debug(
@@ -4168,6 +4189,8 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
                 parent_id=parent_id,
             )
             if ch:
+                if os.environ.get("HERMES_STRICT_LOCAL_MODEL_ROUTING") == "1":
+                    raise RuntimeError("local inference route mismatch")
                 if ch.model:
                     model = ch.model
                 if ch.provider:
@@ -4227,7 +4250,7 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
                     _last_good[resolved_session_key] = model
                 _last_good["*"] = model
 
-        return model, runtime_kwargs
+        return _enforce_strict_local_model_route(model, runtime_kwargs)
 
     def _resolve_turn_agent_config(self, user_message: str, model: str, runtime_kwargs: dict) -> dict:
         """Build the effective model/runtime config for a single turn.
@@ -5404,6 +5427,8 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
     @staticmethod
     def _load_provider_routing() -> dict:
         """Load OpenRouter provider routing preferences from config.yaml."""
+        if os.environ.get("HERMES_STRICT_LOCAL_MODEL_ROUTING") == "1":
+            return {}
         try:
             import yaml as _y
             cfg_path = _hermes_home / "config.yaml"
@@ -5423,6 +5448,8 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
         legacy ``fallback_model`` entries. ``fallback_providers`` stays first
         when both keys are present.
         """
+        if os.environ.get("HERMES_STRICT_LOCAL_MODEL_ROUTING") == "1":
+            return None
         try:
             import yaml as _y
             cfg_path = _hermes_home / "config.yaml"
@@ -5450,6 +5477,9 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
         cached agent's working fallback for that turn.  Only a successful read
         that genuinely lacks the key clears the chain.
         """
+        if os.environ.get("HERMES_STRICT_LOCAL_MODEL_ROUTING") == "1":
+            self._fallback_model = None
+            return None
         try:
             import yaml as _y
             cfg_path = _hermes_home / "config.yaml"
@@ -11079,6 +11109,8 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
             return await self._handle_goal_command(event)
 
         if canonical == "moa":
+            if os.environ.get("HERMES_STRICT_LOCAL_MODEL_ROUTING") == "1":
+                return "Model routing is pinned to local inference."
             # /moa is one-shot sugar only: run a single prompt through the
             # default MoA preset, then restore the prior model. To *switch* to a
             # MoA preset for the session, pick it from the model picker (MoA
@@ -17453,6 +17485,8 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
             return
         if not persisted:
             return
+        if os.environ.get("HERMES_STRICT_LOCAL_MODEL_ROUTING") == "1":
+            raise RuntimeError("local inference route mismatch")
         override: Dict[str, Any] = {
             "model": persisted.get("model"),
             "provider": persisted.get("provider"),
@@ -17502,6 +17536,8 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
             val = override.get(key)
             if val is not None:
                 runtime_kwargs[key] = val
+        if os.environ.get("HERMES_STRICT_LOCAL_MODEL_ROUTING") == "1":
+            return _enforce_strict_local_model_route(model, runtime_kwargs)
         if (
             runtime_kwargs.get("api_key")
             and runtime_kwargs.get("credential_pool") is None
diff --git a/gateway/slash_commands.py b/gateway/slash_commands.py
index 4aa75fa79..7aafb3776 100644
--- a/gateway/slash_commands.py
+++ b/gateway/slash_commands.py
@@ -1444,6 +1444,8 @@ class GatewaySlashCommandsMixin:
           /model <name> --provider <provider> — switch provider + model
           /model --provider <provider>        — switch to provider, auto-detect model
         """
+        if os.environ.get("HERMES_STRICT_LOCAL_MODEL_ROUTING") == "1":
+            return "Model routing is pinned to local inference."
         from gateway.run import _hermes_home, _load_gateway_config
         import yaml
         from hermes_cli.model_switch import (
diff --git a/hermes_cli/runtime_provider.py b/hermes_cli/runtime_provider.py
index 6fee9b013..cab8761f2 100644
--- a/hermes_cli/runtime_provider.py
+++ b/hermes_cli/runtime_provider.py
@@ -1519,6 +1519,38 @@ def _resolve_explicit_runtime(
     return None
 
 
+def _enforce_strict_local_runtime_request(
+    requested: Optional[str],
+    explicit_api_key: Optional[str],
+    explicit_base_url: Optional[str],
+    target_model: Optional[str],
+) -> None:
+    if os.environ.get("HERMES_STRICT_LOCAL_MODEL_ROUTING") != "1":
+        return
+    model_cfg = _get_model_config()
+    expected_model = os.environ.get("HERMES_PINNED_MODEL", "").strip()
+    expected_base_url = os.environ.get("HERMES_PINNED_BASE_URL", "").strip().rstrip("/")
+    configured_provider = str(model_cfg.get("provider") or "").strip().lower()
+    candidate_provider = str(requested or configured_provider).strip().lower()
+    candidate_model = str(target_model or model_cfg.get("default") or "").strip()
+    candidate_base_url = str(
+        explicit_base_url or model_cfg.get("base_url") or ""
+    ).strip().rstrip("/")
+    candidate_api_key = str(
+        explicit_api_key or model_cfg.get("api_key") or ""
+    ).strip()
+    if (
+        configured_provider != "custom"
+        or candidate_provider != "custom"
+        or not expected_model
+        or candidate_model != expected_model
+        or not expected_base_url
+        or candidate_base_url != expected_base_url
+        or candidate_api_key not in {"", "no-key-required"}
+    ):
+        raise RuntimeError("local inference route mismatch")
+
+
 def resolve_runtime_provider(
     *,
     requested: Optional[str] = None,
@@ -1536,6 +1568,12 @@ def resolve_runtime_provider(
     persisted default. Other callers can leave it None to preserve existing
     behavior (api_mode derived from config).
     """
+    _enforce_strict_local_runtime_request(
+        requested,
+        explicit_api_key,
+        explicit_base_url,
+        target_model,
+    )
     requested_provider = resolve_requested_provider(requested)
 
     # Honour ``providers.<name>.enabled: false`` for BOTH user-defined
