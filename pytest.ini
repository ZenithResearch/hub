[pytest]
markers =
    security: marks tests as security-related (matrix production initiative)
    matrix: marks tests related to Matrix/Synapse integration

testpaths = tests
python_files = test_*.py
addopts = -ra -q --strict-markers